# Polynomial

version 1.0
注：运算的加减乘除的对象也可能是多项式
Identify主要识别字符串，把输入的字符串多项式封装成一个多项式链表，返回头节点。
interact主要是写交互界面，运用函数完成计算，以及最后的输出函数
Cal是计算两个多项式的加减乘，计算后记得按照幂次从高到低排序。
幂次相同的时候，x幂次较大的项数在前。
输入格式为 数字x^项数y^项数或数字y^项数x^项数，如25x^5y^-3

------------------------------------------------
version 1.2
框架完成
注：一个封装的多项式，头结点统一为dummy header.